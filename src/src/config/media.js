/**
 * Central place to plug in your own photography.
 *
 * HERO_IMAGE_URL is used as the background on the homepage hero and the
 * login/register side panel, so the whole app stays visually consistent.
 *
 * How to set it:
 *   1) Unsplash/Pexels: open a photo, right-click it > "Copy image address",
 *      paste the URL below.
 *   2) Your own file: drop it in the `public/` folder (e.g. public/hero.jpg)
 *      and set this to "/hero.jpg".
 *
 * Suggested search terms: "students studying", "high school classroom",
 * "teen focused laptop" — bright, warm-toned photos work best with the
 * navy overlay used across the app.
 */
export const HERO_IMAGE_URL = "/hero.jpg";
